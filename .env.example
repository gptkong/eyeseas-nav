# ========================================
# EyeSeas Navigation - 环境变量配置
# ========================================
# 复制此文件为 .env 并填入实际值
# cp .env.example .env

# ========================================
# 数据库配置（必填）
# ========================================
# PostgreSQL 连接字符串
# 格式: postgresql://用户名:密码@主机:端口/数据库名
DATABASE_URL=postgresql://postgres:password@localhost:5432/eyeseas_nav

# ========================================
# 认证配置（必填）
# ========================================
# JWT 密钥 - 用于签名认证令牌
# 生产环境必须使用至少 32 字符的随机字符串
# 生成方式: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# 管理员密码 - 选择以下两种方式之一
# 方式1: 明文密码（简单，适合开发/测试）
ADMIN_PASSWORD=admin123

# 方式2: Bcrypt 哈希密码（推荐，更安全）
# 生成方式: node -e "console.log(require('bcryptjs').hashSync('your-password', 10))"
# ADMIN_PASSWORD_HASH=$2a$10$N9qo8uLOickgx2ZMRZoMyeIjZAgcfl7p92ldGxad68LJZdL17lhWy

# ========================================
# 应用配置（可选）
# ========================================
# 应用标题 - 显示在页面标题和 Logo 旁
NEXT_PUBLIC_APP_TITLE=EyeSeas Navigation

# 应用描述 - 用于 SEO meta 标签
NEXT_PUBLIC_APP_DESCRIPTION=内外网导航管理系统

# ========================================
# 生产环境特殊配置
# ========================================
# Node 环境
# NODE_ENV=production

# 数据库连接池配置（可选，有默认值）
# DB_MAX_CONNECTIONS=10
# DB_IDLE_TIMEOUT=20
