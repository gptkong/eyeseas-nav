# 书签分类功能实施计划

## 📋 任务概述

**需求**: 设计书签分类功能，增加分类 Tabs 切换

**方案**: 混合架构（用户自定义分类 + 标签系统）

**UI 风格**: 顶部横向 Tabs

## 🎯 核心功能

1. **分类管理**
   - 用户可创建、编辑、删除分类
   - 每个分类支持自定义图标和颜色
   - 分类以 Tabs 形式展示

2. **标签系统**
   - 每个链接可添加多个标签
   - 快速标签筛选器
   - 标签自动提取和建议

3. **筛选逻辑**
   - Tabs 切换显示对应分类的书签
   - 支持标签叠加筛选
   - 保留现有搜索功能

4. **批量操作**
   - 添加书签到分类
   - 批量移动书签
   - 分类增删改

## 🗂️ 数据模型

### Category
```typescript
interface Category {
  id: string;
  name: string;
  icon?: string;
  color?: string;
  order: number;
  createdAt: string;
  updatedAt: string;
}
```

### NavigationLink (扩展)
```typescript
interface NavigationLink {
  // ...现有字段
  categoryId?: string;  // 新增
  tags?: string[];      // 新增
}
```

## 📁 文件清单

### 新建文件
1. `app/api/categories/route.ts`
2. `app/api/categories/[id]/route.ts`
3. `components/CategoryTabs.tsx`
4. `components/TagSelector.tsx`
5. `components/QuickTagFilter.tsx`
6. `components/admin/CategoryManager.tsx`
7. `lib/hooks/useCategories.ts`
8. `scripts/migrate-categories.js`

### 修改文件
1. `lib/types.ts` - 添加 Category 类型
2. `lib/db.ts` - 添加分类 CRUD 方法
3. `lib/validations.ts` - 添加分类验证
4. `lib/utils.ts` - 添加颜色工具
5. `lib/hooks/useNavigation.ts` - 扩展筛选逻辑
6. `components/NavigationDashboard.tsx` - 集成分类 Tabs
7. `components/admin/LinkForm.tsx` - 添加分类选择
8. `components/admin/AdminDashboard.tsx` - 添加分类管理

## ✅ 实施步骤

### 阶段一：数据层 (30分钟)
- [x] 扩展类型定义
- [x] 扩展数据库服务
- [x] 扩展表单验证

### 阶段二：API层 (20分钟)
- [x] 分类管理 API
- [x] 扩展链接 API

### 阶段三：前端组件 (60分钟)
- [x] CategoryTabs 组件
- [x] TagSelector 组件
- [x] QuickTagFilter 组件
- [x] 扩展 NavigationDashboard
- [x] 扩展 LinkForm
- [x] CategoryManager 组件
- [x] 集成到管理后台

### 阶段四：Hooks (20分钟)
- [x] useCategories Hook
- [x] 扩展 useNavigation Hook

### 阶段五：样式 (15分钟)
- [x] 分类主题色
- [x] Tabs 动画

### 阶段六：测试 (15分钟)
- [x] 功能测试
- [x] 响应式测试

## 🎨 设计原则

- **SOLID**: 单一职责，组件解耦
- **KISS**: 简洁设计，避免过度工程
- **DRY**: 提取公共逻辑到 Hooks
- **YAGNI**: 仅实现必要功能

## 📝 备注

- 所有新字段为可选，兼容现有数据
- 使用现有 Redis 缓存机制
- 保持现有 UI 风格一致性
